spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://bancow5-database.cwxrdrylwixr.ap-northeast-2.rds.amazonaws.com/bancow5
    username: admin
    password: bancow5!

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org:
      hibernate:
        SQL: debug
        type: trace